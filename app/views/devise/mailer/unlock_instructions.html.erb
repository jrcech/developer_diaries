<p><%= t('devise.mailer.shared.hello', email: @resource.email) %></p>
<p><%= t('.your_account_has_been_locked') %></p>
<p><%= t('.click_the_link_below') %></p>
<p><%= link_to t('.unlock_my_account'), unlock_url(@resource, unlock_token: @token) %></p>
